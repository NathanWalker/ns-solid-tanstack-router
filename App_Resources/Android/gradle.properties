# Increase Gradle HTTP timeouts to avoid transient network read timeouts
# when resolving Android/Maven dependencies during project configuration.
systemProp.org.gradle.internal.http.connectionTimeout=120000
systemProp.org.gradle.internal.http.socketTimeout=120000
